import React, { useState } from 'react';

import {
  ActivityIndicator,
} from 'react-native';

import {
  Container
  Text,
} from './styles';

const ComponentCustom = () => {
  return (
    <Container>
    <Text>oi</Text>
    </Container>
  );
}